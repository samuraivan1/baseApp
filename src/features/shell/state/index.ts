/**
 * Public API for the Shell feature.
 * This is the entry point for all modules that want to interact with the Shell.
 */
export { useAuthStore, getAuthStore } from './authStore';
export type { AuthStoreType } from './store.types';
